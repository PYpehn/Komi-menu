--[[
    WindUI Hub - Full Version (with ImageLabel Background)
]]

local cloneref = (cloneref or clonereference or function(instance)
    return instance
end)
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))
local RunService = cloneref(game:GetService("RunService"))

local WindUI

do
    local ok, result = pcall(function()
        return require("./src/Init")
    end)

    if ok then
        WindUI = result
    else
        if RunService:IsStudio() or not writefile then
            WindUI = require(ReplicatedStorage:WaitForChild("WindUI"):WaitForChild("Init"))
        else
            WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
        end
    end
end

-- ============================================================
--  DEFAULT BACKGROUND (ImageLabel method)
-- ============================================================
local DEFAULT_BG = "https://i.imgur.com/8Km9tLL.jpeg"  -- or rbxassetid://

-- ============================================================
--  CREATE WINDOW
-- ============================================================
local Window = WindUI:CreateWindow({
    Title = "Advanced Hub",
    Author = "by .ftgs",
    Icon = "solar:wind-bold",
    Theme = "Dark",
    ToggleKey = Enum.KeyCode.F,
    -- Built-in background (optional)
    Background = DEFAULT_BG,
    BackgroundTransparency = 0.2,
})

Window:Tag({
    Title = "v2.2",
    Color = "ElementBackground",
})

-- ============================================================
--  MANUAL BACKGROUND (ImageLabel) – WORKS IN WINDUI
-- ============================================================
local function SetManualBackground(url, transparency)
    -- Find the main frame (varies by WindUI version)
    local mainFrame = Window.Holder or Window.Main or Window
    if not mainFrame then return end

    -- Remove old manual background if any
    local oldBg = mainFrame:FindFirstChild("CustomBackground")
    if oldBg then oldBg:Destroy() end

    -- Create new ImageLabel
    local bgImage = Instance.new("ImageLabel")
    bgImage.Name = "CustomBackground"
    bgImage.Image = url
    bgImage.ScaleType = Enum.ScaleType.Crop  -- or Slice/Stretch
    bgImage.Size = UDim2.new(1, 0, 1, 0)
    bgImage.BackgroundTransparency = 1
    bgImage.ImageTransparency = transparency or 0.2
    bgImage.ZIndex = -1  -- behind everything
    bgImage.Parent = mainFrame

    -- Ensure it stays behind (adjust if needed)
    bgImage:SetAttribute("ManualBG", true)
end

-- Apply initial background using the manual method
SetManualBackground(DEFAULT_BG, 0.2)

-- ============================================================
--  CREATE TABS (explicit references)
-- ============================================================

-- ====== MAIN TAB ======
local MainTab = Window:Tab({
    Title = "Main",
    Icon = "warehouse",
})

MainTab:Section({ Title = "Player Settings", Icon = "user" })

MainTab:Slider({
    Title = "Walk Speed",
    Desc = "Adjust movement speed",
    Flag = "walkSpeed",
    Value = { Min = 16, Max = 100, Default = 50 },
    Callback = function(value)
        local plr = game.Players.LocalPlayer
        if plr and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            if hum then hum.WalkSpeed = value end
        end
    end
})

MainTab:Slider({
    Title = "Jump Power",
    Desc = "Adjust jump height",
    Flag = "jumpPower",
    Value = { Min = 50, Max = 200, Default = 50 },
    Callback = function(value)
        local plr = game.Players.LocalPlayer
        if plr and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            if hum then hum.JumpPower = value end
        end
    end
})

MainTab:Divider()

MainTab:Toggle({
    Title = "Infinite Jump",
    Desc = "Jump in mid-air",
    Flag = "infiniteJump",
    Value = false,
    Callback = function(state)
        _G.InfiniteJump = state
    end
})

MainTab:Toggle({
    Title = "Fly Mode",
    Desc = "Enable flight",
    Flag = "flyMode",
    Value = false,
    Callback = function(state)
        local plr = game.Players.LocalPlayer
        if plr and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            if hum then
                hum.PlatformStand = state
                local root = plr.Character:FindFirstChild("HumanoidRootPart")
                if root then root.Anchored = state end
            end
        end
    end
})

-- ====== EXPLOITS TAB ======
local ExploitsTab = Window:Tab({
    Title = "Exploits",
    Icon = "terminal",
})

ExploitsTab:Section({ Title = "Combat", Icon = "sword" })

ExploitsTab:Toggle({
    Title = "Auto Farm",
    Desc = "Automatically attack enemies",
    Flag = "autoFarm",
    Value = false,
    Callback = function(state)
        _G.AutoFarm = state
    end
})

ExploitsTab:Slider({
    Title = "Attack Range",
    Flag = "attackRange",
    Value = { Min = 5, Max = 50, Default = 10 },
    Callback = function(value)
        _G.AttackRange = value
    end
})

ExploitsTab:Divider()

ExploitsTab:Section({ Title = "Movement" })

ExploitsTab:Toggle({
    Title = "Noclip",
    Desc = "Walk through walls",
    Flag = "noclip",
    Value = false,
    Callback = function(state)
        _G.Noclip = state
    end
})

-- ====== AIMBOT TAB ======
local AimbotTab = Window:Tab({
    Title = "Aimbot",
    Icon = "locate-fixed",
})

AimbotTab:Section({ Title = "Aimbot Settings" })

AimbotTab:Toggle({
    Title = "Enable Aimbot",
    Flag = "aimbot",
    Value = false,
    Callback = function(state)
        _G.Aimbot = state
    end
})

AimbotTab:Slider({
    Title = "Smoothness",
    Flag = "aimSmoothness",
    Value = { Min = 1, Max = 100, Default = 50 },
    Callback = function(value)
        _G.AimSmoothness = value
    end
})

AimbotTab:Dropdown({
    Title = "Target Priority",
    Flag = "aimPriority",
    Values = { "Closest", "Lowest Health", "Highest Health" },
    Value = "Closest",
    Callback = function(option)
        _G.AimPriority = option
    end
})

-- ====== ESP TAB ======
local EspTab = Window:Tab({
    Title = "ESP",
    Icon = "eye",
})

EspTab:Section({ Title = "Visuals" })

EspTab:Toggle({
    Title = "ESP",
    Flag = "esp",
    Value = false,
    Callback = function(state)
        _G.ESP = state
    end
})

EspTab:Dropdown({
    Title = "ESP Color",
    Flag = "espColor",
    Values = { "Red", "Blue", "Green", "Yellow", "White", "Pink", "Cyan" },
    Value = "Red",
    Callback = function(option)
        _G.ESPColor = option
    end
})

EspTab:Toggle({
    Title = "Chams",
    Flag = "chams",
    Value = false,
    Callback = function(state)
        _G.Chams = state
    end
})

EspTab:Divider()

EspTab:Slider({
    Title = "Field of View",
    Flag = "fov",
    Value = { Min = 30, Max = 120, Default = 70 },
    Callback = function(value)
        game.Workspace.CurrentCamera.FieldOfView = value
    end
})

-- ====== TELEPORT TAB ======
local TeleportTab = Window:Tab({
    Title = "Teleport",
    Icon = "map-pin",
})

TeleportTab:Section({ Title = "Location Teleport", Icon = "map" })

local Locations = {
    { Name = "Spawn Point", Position = Vector3.new(0, 0, 0) },
    { Name = "Lobby", Position = Vector3.new(100, 10, 50) },
    { Name = "Arena", Position = Vector3.new(-200, 20, -150) },
    { Name = "Shop", Position = Vector3.new(300, 5, -100) },
    { Name = "Boss Area", Position = Vector3.new(500, 50, 200) },
}

local locDropdown = TeleportTab:Dropdown({
    Title = "Select Location",
    Flag = "teleportLocation",
    Values = {},
    Value = "",
    Callback = function(option) end
})

local locNames = {}
for _, loc in ipairs(Locations) do
    table.insert(locNames, loc.Name)
end
locDropdown:Refresh(locNames)
locDropdown:Select(locNames[1] or "")

TeleportTab:Button({
    Title = "Teleport Now",
    Icon = "send",
    IconAlign = "Left",
    Color = Color3.fromHex("#30ff6a"),
    Callback = function()
        local selected = locDropdown:GetValue()
        for _, loc in ipairs(Locations) do
            if loc.Name == selected then
                local plr = game.Players.LocalPlayer
                if plr and plr.Character then
                    local root = plr.Character:FindFirstChild("HumanoidRootPart")
                    if root then
                        root.CFrame = CFrame.new(loc.Position)
                        WindUI:Notify({ Title = "Teleported", Content = "Moved to " .. loc.Name, Icon = "check", Duration = 2 })
                    end
                end
                break
            end
        end
    end
})

TeleportTab:Divider()

TeleportTab:Section({ Title = "Quick Teleport", Icon = "zap" })

for _, loc in ipairs(Locations) do
    TeleportTab:Button({
        Title = loc.Name,
        Icon = "map-pin",
        IconAlign = "Left",
        Callback = function()
            local plr = game.Players.LocalPlayer
            if plr and plr.Character then
                local root = plr.Character:FindFirstChild("HumanoidRootPart")
                if root then
                    root.CFrame = CFrame.new(loc.Position)
                    WindUI:Notify({ Title = "Teleported", Content = "Moved to " .. loc.Name, Icon = "check", Duration = 2 })
                end
            end
        end
    })
end

-- ====== MISC TAB ======
local MiscTab = Window:Tab({
    Title = "Misc",
    Icon = "settings",
})

MiscTab:Section({ Title = "Extra Utilities", Icon = "tool" })

MiscTab:Toggle({
    Title = "God Mode",
    Desc = "Become invincible",
    Flag = "godMode",
    Value = false,
    Callback = function(state)
        _G.GodMode = state
        local plr = game.Players.LocalPlayer
        if plr and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            if hum then
                hum.MaxHealth = state and math.huge or 100
                hum.Health = state and math.huge or 100
            end
        end
    end
})

MiscTab:Toggle({
    Title = "Infinite Stamina",
    Desc = "Never run out of stamina",
    Flag = "infiniteStamina",
    Value = false,
    Callback = function(state)
        _G.InfiniteStamina = state
    end
})

MiscTab:Toggle({
    Title = "Speed Boost",
    Desc = "Multiply your speed by 1.5x",
    Flag = "speedBoost",
    Value = false,
    Callback = function(state)
        _G.SpeedBoost = state
    end
})

MiscTab:Divider()

MiscTab:Section({ Title = "Notifications" })

MiscTab:Button({
    Title = "Test Notification",
    Icon = "bell",
    Callback = function()
        WindUI:Notify({
            Title = "Test",
            Content = "This is a test notification!",
            Icon = "bell",
            Duration = 3
        })
    end
})

-- ====== STATS TAB ======
local StatsTab = Window:Tab({
    Title = "Stats",
    Icon = "bar-chart-3",
})

StatsTab:Section({ Title = "Live Player Stats", Icon = "activity" })

local statsPara = StatsTab:Paragraph({
    Title = "Player Information",
    Desc = "Loading...",
    Image = "user",
    ImageSize = 20,
    Color = "White",
})

task.spawn(function()
    while true do
        local plr = game.Players.LocalPlayer
        if plr and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            local root = plr.Character:FindFirstChild("HumanoidRootPart")
            if hum and root then
                local health = math.floor(hum.Health)
                local maxHealth = math.floor(hum.MaxHealth)
                local speed = math.floor(hum.WalkSpeed)
                local pos = root.Position
                statsPara:SetDesc(string.format(
                    "Health: %d / %d\nWalk Speed: %d\nPosition: (%.1f, %.1f, %.1f)",
                    health, maxHealth, speed, pos.X, pos.Y, pos.Z
                ))
            end
        end
        task.wait(1)
    end
end)

-- ====== SETTINGS TAB ======
local SettingsTab = Window:Tab({
    Title = "Settings",
    Icon = "settings",
})

SettingsTab:Section({ Title = "Background Customization", Icon = "image" })

-- Input for background URL
local bgInput = SettingsTab:Input({
    Title = "Background URL",
    Desc = "Paste image URL or Roblox asset ID",
    Icon = "link",
    Placeholder = "https://example.com/image.png",
    Value = DEFAULT_BG,
    Callback = function(text) end
})

-- Apply button (manual ImageLabel method)
SettingsTab:Button({
    Title = "Apply Background",
    Icon = "check",
    IconAlign = "Left",
    Color = Color3.fromHex("#315dff"),
    Callback = function()
        local url = bgInput:GetValue()
        if url and url ~= "" then
            -- Use manual method
            local success, err = pcall(function()
                SetManualBackground(url, 0.2)
                -- Also update built-in background if available
                pcall(function() Window:SetBackgroundImage(url) end)
            end)
            if success then
                WindUI:Notify({ Title = "Background Updated", Content = "New background applied!", Icon = "check", Duration = 2 })
            else
                WindUI:Notify({ Title = "Error", Content = "Failed to set background: " .. tostring(err), Icon = "x", Duration = 3 })
            end
        end
    end
})

-- Transparency slider for manual background
SettingsTab:Slider({
    Title = "Background Transparency",
    Desc = "0 = opaque, 1 = fully transparent",
    Flag = "bgTransparency",
    Value = { Min = 0, Max = 1, Default = 0.2 },
    Step = 0.05,
    Callback = function(value)
        -- Update manual background if exists
        local mainFrame = Window.Holder or Window.Main or Window
        if mainFrame then
            local bg = mainFrame:FindFirstChild("CustomBackground")
            if bg then
                bg.ImageTransparency = value
            end
        end
        -- Also update built-in
        pcall(function() Window:SetBackgroundImageTransparency(value) end)
    end
})

SettingsTab:Divider()

SettingsTab:Section({ Title = "Preset Backgrounds", Icon = "palette" })

local Presets = {
    { Name = "Space", URL = "https://i.imgur.com/8Km9tLL.jpeg" },
    { Name = "Neon City", URL = "https://i.imgur.com/ZNz3XQx.jpeg" },
    { Name = "Dark Gradient", URL = "rbxassetid://10618928818" },
    { Name = "Cyberpunk", URL = "https://i.imgur.com/1J5Qk2v.jpeg" },
    { Name = "Sunset", URL = "https://i.imgur.com/3r8nHr7.jpeg" },
    { Name = "Abstract", URL = "https://i.imgur.com/2BqYlQk.jpeg" },
}

for _, preset in ipairs(Presets) do
    SettingsTab:Button({
        Title = preset.Name,
        Icon = "image",
        IconAlign = "Left",
        Callback = function()
            pcall(function()
                SetManualBackground(preset.URL, 0.2)
                pcall(function() Window:SetBackgroundImage(preset.URL) end)
                bgInput:SetValue(preset.URL)
                WindUI:Notify({ Title = "Preset Applied", Content = preset.Name .. " background set!", Icon = "check", Duration = 2 })
            end)
        end
    })
end

-- ====== INFO TAB ======
local InfoTab = Window:Tab({
    Title = "Info",
    Icon = "badge-info",
})

InfoTab:Paragraph({
    Title = "About",
    Desc = "Advanced Hub - Built with WindUI\n\nFeatures:\n• Player control (speed, jump, fly)\n• Combat (auto farm, range)\n• Aimbot with priority\n• ESP & Chams\n• FOV adjustment\n• Teleport to locations\n• Misc utilities (God Mode, Stamina)\n• Live stats display\n• Custom backgrounds (ImageLabel method)",
    Buttons = {
        {
            Title = "GitHub",
            Callback = function()
                setclipboard("https://github.com/Footagesus/WindUI")
                WindUI:Notify({ Title = "Copied!", Content = "GitHub link copied", Duration = 2 })
            end,
        },
        {
            Title = "Documentation",
            Variant = "Secondary",
            Callback = function()
                print("Documentation requested")
            end,
        },
    },
})

local HStack = InfoTab:HStack()
local VStackLeft = HStack:VStack()
local VStackRight = HStack:VStack()

VStackLeft:Button({
    Title = "Reload UI",
    Justify = "Center",
    Icon = "refresh-ccw",
    IconAlign = "Left",
    Color = Color3.fromHex("#F44732"),
    Callback = function()
        print("Reloading UI...")
    end,
})

VStackRight:Button({
    Title = "Rejoin Place",
    Justify = "Center",
    Icon = "log-out",
    IconAlign = "Left",
    Color = Color3.fromHex("#f4b332"),
    Callback = function()
        game:GetService("TeleportService"):Teleport(game.PlaceId)
    end,
})

-- ============================================================
--  AUTO-SAVE CONFIG
-- ============================================================
local ConfigManager = Window.ConfigManager
if ConfigManager then
    ConfigManager:Init(Window)
    ConfigManager:RegisterFlag("walkSpeed")
    ConfigManager:RegisterFlag("jumpPower")
    ConfigManager:RegisterFlag("infiniteJump")
    ConfigManager:RegisterFlag("flyMode")
    ConfigManager:RegisterFlag("autoFarm")
    ConfigManager:RegisterFlag("attackRange")
    ConfigManager:RegisterFlag("noclip")
    ConfigManager:RegisterFlag("aimbot")
    ConfigManager:RegisterFlag("aimSmoothness")
    ConfigManager:RegisterFlag("aimPriority")
    ConfigManager:RegisterFlag("esp")
    ConfigManager:RegisterFlag("espColor")
    ConfigManager:RegisterFlag("chams")
    ConfigManager:RegisterFlag("fov")
    ConfigManager:RegisterFlag("godMode")
    ConfigManager:RegisterFlag("infiniteStamina")
    ConfigManager:RegisterFlag("speedBoost")
    ConfigManager:RegisterFlag("bgTransparency")
end

-- ============================================================
--  KEYBIND (already set to F, but we can add more)
-- ============================================================
-- Add a custom keybind to toggle Fly Mode (optional)
local UserInputService = game:GetService("UserInputService")
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.F then
        -- Toggle fly mode (example)
        local flyToggle = MainTab:GetElement("flyMode")
        if flyToggle then
            flyToggle:Toggle()
        end
    end
end)

-- ============================================================
--  STARTUP NOTIFICATION
-- ============================================================
WindUI:Notify({
    Title = "Hub Loaded",
    Content = "All features ready! Press F to toggle fly mode.",
    Icon = "sparkles",
    Duration = 5
})

print("✅ WindUI Hub loaded successfully!")
